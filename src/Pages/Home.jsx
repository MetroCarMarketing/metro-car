import React from "react";
import Landing from "../Components/Landing";

const Home = ({ translation, setActive }) => {
  return <Landing translation={translation} setActive={setActive} />;
};

export default Home;
