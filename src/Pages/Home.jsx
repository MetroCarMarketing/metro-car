import React from "react";
import "../CSS/Home.css";

import Landing from "../Components/Landing";

const Home = ({ content }) => {
  return (
<Landing content={content} />
  );
};

export default Home;
